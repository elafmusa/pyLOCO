[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyLOCO"
version = "0.3.0"

[project.optional-dependencies]
gui = ["PySide6"]

[project.scripts]
pyloco-gui = "pyLOCO.gui.app:main"
